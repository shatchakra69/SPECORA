name: Keep backend awake

# Render's free tier sleeps the server after ~15 min of inactivity, which causes
# a slow cold start on the next request. This pings the health endpoint on a
# schedule so the server stays warm during the day.

on:
  schedule:
    - cron: "*/10 * * * *" # every 10 minutes
  workflow_dispatch: {} # allow manual runs from the Actions tab

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend (Render) health endpoint
        run: |
          curl -sS -m 60 -o /dev/null -w "render health: HTTP %{http_code} in %{time_total}s\n" \
            https://blc-server-yp7x.onrender.com/api/health

      - name: Ping Supabase (resets the free-tier inactivity pause timer)
        run: |
          curl -sS -m 60 -o /dev/null -w "supabase health: HTTP %{http_code} in %{time_total}s\n" \
            https://josbdfqeuoibqjqwstug.supabase.co/auth/v1/health || true
